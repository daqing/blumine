<div class="conversation">
  <p><%= conversation.user.name %>开始了新会话</p>
  <div class="replies">
    <%= render conversation.replies %>
  </div>
  <% reply = conversation.replies.build %>
  <%= form_for [project, conversation, reply], :html => {:multipart => true} do |f| %>
    <%= render 'replies/fields', :f => f %>
    <%= f.submit %>
  <% end %>
</div>
