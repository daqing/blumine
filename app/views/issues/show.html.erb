<h2><%= @issue.title %></h2>
<p><%=raw parse_markdown h(@issue.content) %></p>

<div class="bar"><h3>评论</h3></div>
<%= render @issue.comments %>

<hr>
<%= form_for [@issue, @comment] do |f| %>
  <div class="field">
    <%= f.text_area :content %>
  </div>
  <%= f.submit "发表评论" %>
<% end %>

