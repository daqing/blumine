<%= form_for @project do |f| %>

  <%= render 'shared/error_messages', :obj => @project %>
<fieldset>
    <%= f.label :name, t(:project_name) %>
    <%= f.text_field :name %>

</fieldset>  <%= f.submit t(:create_project) %>

<% end %>

