<% content_for :content_area do %>
  <h3>
  项目文档
  <%= link_to '新建文档', new_project_document_path(@project) %>
  </h3>

  <div class="documents">
    <%= render @documents, :project => @project %>
  </div>
<% end %>

<%= render 'shared/project_page' %>
